import $ from 'jquery';

class MegaMenuHeading {
  constructor({ el }) {
    this.root = $(el);
  }
  cache = {};
  initCache() {
    /* */
  }


  bindEvents() {
    /* */
  }

  init() {
    this.initCache();
    this.bindEvents();
  }
}

export default MegaMenuHeading;
