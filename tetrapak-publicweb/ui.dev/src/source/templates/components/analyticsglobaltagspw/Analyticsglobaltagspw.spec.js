import Analyticsglobaltagspw from './Analyticsglobaltagspw';

describe('Analyticsglobaltagspw', function () {
  before(function () {
    this.analyticsglobaltagspw = new Analyticsglobaltagspw({
      el: document.body
    });
  });
});
