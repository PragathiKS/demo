//import Breadcrumb from './Breadcrumb';
//import $ from 'jquery';
//import breadcrumbTemplate from '../../../test-templates-hbs/breadcrumb.hbs';

describe('Breadcrumb', function () {
  before(function () {
    // $(document.body).empty().html(breadcrumbTemplate());
    // this.breadcrumb = new Breadcrumb({ el: document.body });
    // this.initSpy = sinon.spy(this.breadcrumb, 'init');
    // //this.openMobileMenuBoxToggleSpy = sinon.spy(this.breadcrumb, 'openMobileMenuBoxToggle');
    // //this.hideMobileMenuOnResizeSpy = sinon.spy(this.breadcrumb, 'hideMobileMenuOnResize');
    // this.trackAnalyticsSpy = sinon.spy(this.breadcrumb, 'trackAnalytics');
    // window.digitalData = {};
    // window._satellite = {
    //   track() { /* Dummy method */ }
    // };
    // this.header.init();

  });
  after(function () {
    // $(document.body).empty();
    // this.initSpy.restore();
    // // this.openMobileMenuBoxToggleSpy.restore();
    // //this.hideMobileMenuOnResizeSpy.restore();
    // this.trackAnalyticsSpy.restore();
    //this.replaceStub.restore();
  });
//   it('should initialize', function () {
//     expect(this.header.init.called).to.be.true;
//   });
  
//   it('should call track analytics on click', function () {
//     $('.js-tp-pw-header-logo-digital-data').trigger('click');
//     expect(this.header.trackAnalytics.called).to.be.true;
//   });

//   it('should set digitalData after track analytics call', function() {
//     expect(window.digitalData.linkClick.linkType).to.equal('internal');
//   });

});
