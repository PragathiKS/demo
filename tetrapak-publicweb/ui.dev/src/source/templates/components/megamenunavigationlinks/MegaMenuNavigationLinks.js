import $ from 'jquery';

class MegaMenuNavigationLinks {
  constructor({ el }) {
    this.root = $(el);
  }
  cache = {};
  
  initCache() {
    /* */
  }

  bindEvents() {
    /* */
  }

  init() {
    this.initCache();
    this.bindEvents();
  }
}

export default MegaMenuNavigationLinks;
