import $ from 'jquery';

class MegaMenuDescription {
  constructor({ el }) {
    this.root = $(el);
  }
  
  bindEvents() {
    /* */
  }

  init() {
    this.bindEvents();
  }
}

export default MegaMenuDescription;
