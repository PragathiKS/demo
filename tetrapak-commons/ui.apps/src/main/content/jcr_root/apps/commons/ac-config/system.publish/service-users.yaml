- user_config:

    - tetrapak-gl-reader:

        - name:
          path: tetrapak
          isSystemUser: true

- ace_config:

    - tetrapak-gl-reader:

        - path: /etc/acs-commons/lists
          permission: allow
          actions: read
          privileges: