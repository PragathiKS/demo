{{#if iconClass}}<div class="tpatom-input form-element{{#if wrapClasses}} {{wrapClasses}}{{/if}}">{{/if}}
    <input type="text" name="{{name}}" class="form-control{{#if eleClasses}} {{eleClasses}}{{/if}}"
        {{#if dataLocator}}
            data-locator="{{dataLocator}}"
        {{/if}}
        {{#if readonly}}
            readonly="readonly"
        {{/if}}
        value="{{#if defaultValue}}{{i18n key=defaultValue}}{{/if}}"
        {{#if label}}
            aria-label="{{i18n key=label}}"
        {{/if}}
        {{#if placeholder}}
            placeholder="{{i18n key=placeholder}}"
        {{/if}}
    />
    {{#if iconClass}}
        <i class="form-element__icon {{iconClass}}"></i>
    {{/if}}
{{#if iconClass}}</div>{{/if}}
